{% extends "base.html.twig" %}
{% block title %}Show user{% endblock %}
{% block body %}
    {% block nav %}
        {{ parent() }}
    {% endblock %}
    <div class="container" id="front-show-user">
        <h3>Información del usuario</h3>
        <div class="row">
            <div class="col-3">
                <b>Usuario: </b>{{ app.user.username }}
            </div>
            <div class="col-3">
               <b>Email: </b> {{ app.user.email }}
            </div>
            <div class="col-3">
                <b>Tipo de usuario: </b> {{ app.user.type }}
            </div>
        </div>
    </div>

{% endblock %}